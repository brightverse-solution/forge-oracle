{
  "name": "forge-oracle",
  "version": "0.1.0",
  "description": "FORGE Oracle — CLI: CSV → Excel quotation generator",
  "type": "module",
  "private": true,
  "scripts": {
    "gen-quote": "bun run tools/quote-generator/src/cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "dependencies": {
    "commander": "^12.1.0",
    "exceljs": "^4.4.0",
    "papaparse": "^5.4.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.1.14",
    "@types/node": "^22.10.0",
    "@types/papaparse": "^5.3.15",
    "typescript": "^5.7.2"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
